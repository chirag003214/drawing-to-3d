# --- IR + API (Steps 1-3) ---
fastapi
uvicorn[standard]
sqlalchemy
python-multipart        # file uploads (Slice A)

# --- CV pipeline (Slice B, Step 5) ---
opencv-python-headless
numpy
pillow
pytesseract             # fallback OCR engine; also needs the system `tesseract-ocr` binary
easyocr                 # preferred OCR engine for dimension text (app/ocr_dims.py).
                        # NOTE: pulls in PyTorch -- expect a ~2GB download/install.
                        # If easyocr fails to import, the pipeline falls back to
                        # pytesseract automatically; if neither works, dimension
                        # OCR degrades to "no dimensions found" rather than crashing.

# Step 1-2 (IR + seed test) only require: sqlalchemy
